class LibraryClass():
    books =[]
    no_of_books = 0
    def printBooks(self):
        for i in self.books:
            print(i)

    def addBooks(self,book):

        self.books.append(book)
        self.no_of_books += 1



    def getNumber(self):
        return self.no_of_books

    

# books , no_of_books
i = LibraryClass()
cont = True
while ( cont == True):
    a = input("Enter name of book you want to add : ")
    i.addBooks(a)
    c = input("Do you want to add more (Y/N)? ")

    if( c.upper() =="Y"):
        cont= True
    else:
        cont = False
print(i.getNumber())
i.printBooks()